services:
  - type: web
    name: arbitraje-pro
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn arbitraje_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ADMIN_PASSWORD
        value: Mau10
      - key: DB_PATH
        value: /opt/render/project/src/arbitraje.db
